---
phase: 05-field-atlas
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/css/astro-atlas.css
  - _includes/astro-head.html
autonomous: true
requirements:
  - ATLAS-01
  - ATLAS-02

must_haves:
  truths:
    - "Under html.theme-atlas, astrophotography pages show a cream/warm-paper background (#ede6d3) with cartographic grid overlay"
    - "Under html.theme-atlas, the listing and post wrappers use the Italiana display font and JetBrains Mono kicker typography"
    - "The atlas CSS file is loaded on every astrophotography page via astro-head.html"
    - "All atlas CSS is scoped under html.theme-atlas — no styles leak to html.theme-night or non-astro pages"
  artifacts:
    - path: "assets/css/astro-atlas.css"
      provides: "All Field Atlas layout, typography, chart, and grid CSS"
      min_lines: 200
    - path: "_includes/astro-head.html"
      provides: "Link tag for astro-atlas.css"
      contains: "astro-atlas.css"
  key_links:
    - from: "_includes/astro-head.html"
      to: "assets/css/astro-atlas.css"
      via: "<link rel=\"stylesheet\">"
      pattern: "astro-atlas\\.css"
    - from: "html.theme-atlas"
      to: ".al-listing"
      via: "scoped CSS selector"
      pattern: "html\\.theme-atlas .al-listing"
    - from: "html.theme-atlas"
      to: ".db-wrap"
      via: "scoped CSS selector"
      pattern: "html\\.theme-atlas .db-wrap"
---

<objective>
Create `assets/css/astro-atlas.css` with all Field Atlas layout CSS for both the listing page and the post page, then wire it into `_includes/astro-head.html` so it loads on every astrophotography page.

Purpose: This is the CSS foundation that Plans 05-02 and 05-03 depend on. The markup those plans add will be invisible and broken without these styles.

Output:
- `assets/css/astro-atlas.css` — scoped under `html.theme-atlas`, all layout, typography, chart, index-table, two-column post, ribbon, and mobile-stack rules
- `_includes/astro-head.html` — one new `<link>` tag for `astro-atlas.css`
</objective>

<execution_context>
@/Users/chaitanya/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chaitanya/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-field-atlas/05-CONTEXT.md

Design reference JSX (concept-atlas.jsx): /tmp/astro-blog-post/project/concept-atlas.jsx
Design reference CSS (refresh.css): /tmp/astro-blog-post/project/refresh.css
Existing tokens: assets/css/astro-themes.css
Head include: _includes/astro-head.html

<interfaces>
<!-- Tokens available from assets/css/astro-themes.css under html.theme-atlas -->
<!-- Use these custom properties throughout astro-atlas.css — do not redeclare them -->

html.theme-atlas {
  --paper:     #ede6d3;
  --paper-d:   #ddd4be;
  --bg:        #ede6d3;
  --ink:       #1a1f2b;
  --ink-mid:   #4a4a4a;
  --ink-sub:   #786c54;
  --rule:      rgba(26,31,43,0.18);
  --rule-soft: rgba(26,31,43,0.08);
  --accent:    #a04a2e;    /* burnt sienna */
  --indigo:    #2c3a5a;
  --mono:      "JetBrains Mono", ui-monospace, monospace;
  --serif:     "Spectral", Georgia, serif;
  --display:   "Italiana", "Bodoni Moda", serif;
}

<!-- Existing class wrappers in the HTML files -->
<!-- astrophotography.html: .astro-listing-wrap > .al-listing -->
<!-- _layouts/astro-workflow.html: .db-wrap > .db-hero / .db-body -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create assets/css/astro-atlas.css</name>
  <files>assets/css/astro-atlas.css</files>
  <action>
Create a new file `assets/css/astro-atlas.css`. All rules must be scoped under either `html.theme-atlas .al-listing` (listing page) or `html.theme-atlas .db-wrap` (post page). Use `var(--paper)`, `var(--ink)`, `var(--accent)`, `var(--serif)`, `var(--display)`, `var(--mono)` tokens defined in `astro-themes.css` — do not redeclare them here.

File header comment: `/* astro-atlas.css — Field Atlas light theme layout (Phase 5). All rules scoped under html.theme-atlas. Tokens from astro-themes.css. */`

Write the following rule groups in order:

**A. LISTING PAGE — Base and background** (scope: `html.theme-atlas .al-listing`)

Reset the listing wrapper: set `background: var(--paper)`, `color: var(--ink)`, `font-family: var(--serif)`, `position: relative`, `min-height: 100vh`. Add a cartographic grid overlay using a `::before` pseudo-element on `.al-listing` (cannot use a child element since Plan 05-02 adds `.al-grid-bg`): `position: absolute; inset: 0; pointer-events: none; z-index: 0; background-image: linear-gradient(to right, var(--rule-soft) 1px, transparent 1px), linear-gradient(to bottom, var(--rule-soft) 1px, transparent 1px); background-size: 48px 48px; opacity: 0.6`. Also add a paper grain effect via `.al-listing::after`: `position: absolute; inset: 0; pointer-events: none; z-index: 0; background-image: radial-gradient(circle at 15% 20%, rgba(0,0,0,0.04), transparent 40%), radial-gradient(circle at 80% 70%, rgba(0,0,0,0.05), transparent 40%); mix-blend-mode: multiply`. Ensure `.al-listing > *` get `position: relative; z-index: 1` so content sits above the pseudo-element overlays.

**B. LISTING PAGE — Ribbon** (scope: `html.theme-atlas .al-listing`)

`.al-ribbon`: `display: flex; align-items: center; justify-content: space-between; border-bottom: 1px solid var(--ink); padding: 14px 36px; font-family: var(--mono); font-size: 11px; letter-spacing: 0.18em; text-transform: uppercase`.
`.al-ribbon-left`: `display: flex; gap: 36px; align-items: center`.
`.al-ribbon-right`: `color: var(--ink-sub)`.
`.al-ribbon-dot`: `width: 6px; height: 6px; background: var(--accent); display: inline-block; margin-right: 8px; vertical-align: middle; border-radius: 50%`.
`.al-ribbon a`: `color: inherit; text-decoration: none`.

**C. LISTING PAGE — Title / kicker / lede** (scope: `html.theme-atlas .al-listing`)

`.al-atlas-body`: `padding: 44px 64px 0; display: grid; grid-template-columns: 1.05fr 1fr; gap: 80px`.
`.al-atlas-kicker`: `font-family: var(--mono); font-size: 11px; letter-spacing: 0.22em; text-transform: uppercase; color: var(--ink-sub); margin-bottom: 18px`.
`.al-atlas-h1`: `font-family: var(--display); font-size: clamp(64px, 8vw, 120px); line-height: 0.88; letter-spacing: -0.02em; color: var(--ink); font-weight: 400`.
`.al-atlas-h1 em`: `font-family: "Newsreader", serif; font-style: italic; font-weight: 400; color: var(--accent)`.
`.al-atlas-lede`: `font-family: var(--serif); font-size: 17px; line-height: 1.55; color: var(--ink-mid); max-width: 52ch; font-style: italic; margin-top: 28px`.

**D. LISTING PAGE — Atlas index table** (scope: `html.theme-atlas .al-listing`)

`.atlas-index`: `width: 100%; font-family: var(--mono); font-size: 11.5px; margin-top: 44px`.
`.atlas-index .ai-header-row`: `display: grid; grid-template-columns: 36px 1fr 1.6fr 78px 70px 56px 28px; align-items: center; gap: 14px; padding: 10px 0 10px; border-top: 1px solid var(--ink); border-bottom: 1px solid var(--rule); color: var(--ink-sub); letter-spacing: 0.18em; font-size: 10px; text-transform: uppercase`.
`.atlas-index .ai-row`: `display: grid; grid-template-columns: 36px 1fr 1.6fr 78px 70px 56px 28px; align-items: center; gap: 14px; padding: 13px 0; border-bottom: 1px solid var(--rule); text-decoration: none; color: inherit; cursor: pointer; transition: background 0.15s`.
`.atlas-index .ai-row:hover`: `background: rgba(160,74,46,0.06)`.
`.atlas-index .ai-num`: `color: var(--ink-sub); letter-spacing: 0.1em`.
`.atlas-index .ai-desig`: `color: var(--accent); font-weight: 500; letter-spacing: 0.05em`.
`.atlas-index .ai-title`: `font-family: var(--serif); font-size: 16px; font-weight: 500; letter-spacing: 0; text-transform: none; color: var(--ink)`.
`.atlas-index .ai-meta`: `color: var(--ink-mid)`.
`.atlas-index .ai-type`: `font-size: 10px; letter-spacing: 0.12em; text-transform: uppercase; color: var(--ink-mid)`.
`.atlas-index .ai-arrow`: `color: var(--ink-sub); justify-self: end`.

**E. LISTING PAGE — Chart plate (right column)** (scope: `html.theme-atlas .al-listing`)

`.al-chart-col`: `display: flex; flex-direction: column`.
`.al-chart-kicker-row`: `display: flex; justify-content: space-between; margin-bottom: 14px; font-family: var(--mono); font-size: 11px; letter-spacing: 0.22em; text-transform: uppercase; color: var(--ink-sub)`.
`.al-chart`: `position: relative; height: 480px; background: var(--paper-d); border: 1px solid var(--ink)`.
`.al-chart-grid`: `position: absolute; inset: 0; background-image: linear-gradient(to right, rgba(26,31,43,0.12) 1px, transparent 1px), linear-gradient(to bottom, rgba(26,31,43,0.12) 1px, transparent 1px); background-size: 60px 40px`.
`.al-chart-stars`: `position: absolute; inset: 0; background-image: radial-gradient(1px 1px at 12% 22%, rgba(26,31,43,0.6), transparent 50%), radial-gradient(1.5px 1.5px at 28% 60%, rgba(26,31,43,0.4), transparent 50%), radial-gradient(1px 1px at 45% 30%, rgba(26,31,43,0.5), transparent 50%), radial-gradient(1px 1px at 65% 75%, rgba(26,31,43,0.45), transparent 50%), radial-gradient(2px 2px at 80% 35%, rgba(26,31,43,0.55), transparent 50%), radial-gradient(1px 1px at 35% 88%, rgba(26,31,43,0.4), transparent 50%)`.
`.al-chart-corner`: `position: absolute; width: 10px; height: 10px; border: 1px solid var(--ink)`.
`.al-chart-corner.tl`: `top: -1px; left: -1px; border-right: none; border-bottom: none`.
`.al-chart-corner.tr`: `top: -1px; right: -1px; border-left: none; border-bottom: none`.
`.al-chart-corner.bl`: `bottom: -1px; left: -1px; border-right: none; border-top: none`.
`.al-chart-corner.br`: `bottom: -1px; right: -1px; border-left: none; border-top: none`.
`.al-chart-axis`: `position: absolute; font-family: var(--mono); font-size: 9.5px; letter-spacing: 0.1em; color: var(--ink-sub)`.
`.al-chart-marker`: `position: absolute; transform: translate(-50%, -50%); display: flex; flex-direction: column; align-items: flex-start; gap: 6px`.
`.al-chart-marker .cross`: `width: 22px; height: 22px; position: relative; transform: translateX(-11px)`.
`.al-chart-marker .cross::before, .al-chart-marker .cross::after`: `content: ""; position: absolute; background: var(--accent)`.
`.al-chart-marker .cross::before`: `left: 50%; top: 0; bottom: 0; width: 1px; transform: translateX(-50%)`.
`.al-chart-marker .cross::after`: `top: 50%; left: 0; right: 0; height: 1px; transform: translateY(-50%)`.
`.al-chart-marker .ring`: `position: absolute; left: 50%; top: 11px; transform: translate(-50%, -50%); width: 14px; height: 14px; border: 1px solid var(--accent); border-radius: 50%`.
`.al-chart-marker .lbl`: `font-family: var(--mono); font-size: 9.5px; letter-spacing: 0.1em; background: var(--paper); padding: 2px 6px; border: 1px solid var(--ink); text-transform: uppercase; margin-left: 12px; white-space: nowrap`.
`.al-chart-compass`: `position: absolute; right: 14px; bottom: 12px; font-family: var(--mono); font-size: 10px; letter-spacing: 0.1em; color: var(--ink-sub); text-align: right; line-height: 1.5`.
`.al-chart-footnotes`: `display: flex; justify-content: space-between; margin-top: 16px; font-family: var(--mono); font-size: 10px; letter-spacing: 0.14em; text-transform: uppercase; color: var(--ink-sub)`.
`.al-chart-apparatus`: `margin-top: 42px; border-top: 1px solid var(--ink); padding-top: 22px`.
`.al-chart-apparatus-grid`: `display: grid; grid-template-columns: 1fr 1fr; gap: 22px; font-family: var(--serif); font-size: 13.5px; line-height: 1.55; color: var(--ink-mid)`.
`.al-chart-apparatus-label`: `font-family: var(--mono); font-size: 10px; letter-spacing: 0.18em; color: var(--accent); margin-bottom: 6px; text-transform: uppercase`.

**F. LISTING PAGE — Bottom folio strip** (scope: `html.theme-atlas .al-listing`)

`.al-atlas-folio`: `margin: 0 36px; display: flex; justify-content: space-between; border-top: 1px solid var(--ink); padding: 10px 0 16px; font-family: var(--mono); font-size: 10px; letter-spacing: 0.18em; text-transform: uppercase; color: var(--ink-sub)`.

**G. POST PAGE — Base and ribbon** (scope: `html.theme-atlas .db-wrap`)

`.db-wrap` under atlas: `background: var(--paper); color: var(--ink); font-family: var(--serif); position: relative`.
`.db-wrap::before` (cartographic grid): same `background-image` / `background-size` as listing `::before`. Use `position: absolute; inset: 0; pointer-events: none; z-index: 0`.
`.db-wrap > *`: `position: relative; z-index: 1`.

`.db-atlas-ribbon`: `display: flex; align-items: center; justify-content: space-between; border-bottom: 1px solid var(--ink); padding: 14px 36px; font-family: var(--mono); font-size: 11px; letter-spacing: 0.18em; text-transform: uppercase`.
`.db-atlas-ribbon-left`: `display: flex; gap: 36px; align-items: center`.
`.db-atlas-ribbon-right`: `color: var(--ink-sub)`.
`.db-atlas-ribbon a`: `color: inherit; text-decoration: none`.

**H. POST PAGE — Two-column body** (scope: `html.theme-atlas .db-wrap`)

`.db-atlas-cols`: `padding: 36px 64px; display: grid; grid-template-columns: 1.05fr 1fr; gap: 70px; align-items: start`.

`.db-atlas-kicker`: same as `.al-atlas-kicker` (mono 11px, 0.22em tracking, uppercase, `--ink-sub`). `.db-atlas-kicker-row`: `display: flex; justify-content: space-between; margin-bottom: 10px`.

**I. POST PAGE — Left column: chart plate** (scope: `html.theme-atlas .db-wrap`)

`.db-atlas-chart`: `position: relative; border: 1px solid var(--ink); background: var(--paper-d); overflow: hidden`. Height: 380px.
`.db-atlas-chart-grid`: same grid background as `.al-chart-grid`.
`.db-atlas-chart-svg`: `position: absolute; inset: 12px; opacity: 0.85; filter: saturate(0.7)`.
`.db-atlas-chart-ra`: `position: absolute; left: 12px; top: 12px; font-family: var(--mono); font-size: 10px; letter-spacing: 0.14em; color: var(--accent); text-transform: uppercase; line-height: 1.6`.
`.db-atlas-chart-mag`: `position: absolute; right: 12px; top: 12px; font-family: var(--mono); font-size: 10px; letter-spacing: 0.14em; color: var(--ink-sub); text-transform: uppercase; text-align: right; line-height: 1.6`.
`.db-atlas-scale`: `position: absolute; left: 50%; bottom: 18px; transform: translateX(-50%); font-family: var(--mono); font-size: 10px; letter-spacing: 0.14em; color: var(--ink-sub); display: flex; align-items: center; gap: 10px`.
`.db-atlas-scale-bar`: `width: 60px; height: 1px; background: var(--ink); display: inline-block`.

Corner elements: same `.al-chart-corner` patterns, reuse under `.db-atlas-chart .al-chart-corner`.

**J. POST PAGE — Acquisition ledger table** (scope: `html.theme-atlas .db-wrap`)

`.db-atlas-ledger`: `margin-top: 26px`.
`.db-atlas-ledger table`: `width: 100%; font-family: var(--mono); font-size: 11.5px; border-collapse: collapse`.
`.db-atlas-ledger td`: `padding: 8px 0; border-bottom: 1px solid var(--rule)`.
`.db-atlas-ledger .ak`: `color: var(--ink-sub); text-transform: uppercase; letter-spacing: 0.14em; font-size: 10px`.
`.db-atlas-ledger .av`: `color: var(--ink); text-align: right`.

**K. POST PAGE — Right column: prose and sidenotes** (scope: `html.theme-atlas .db-wrap`)

`.db-atlas-prose-col`: `position: relative`.
`.db-atlas-session-kicker`: same as `.db-atlas-kicker`, `margin-bottom: 14px`.
`.db-atlas-h1`: `font-family: var(--display); font-size: clamp(36px, 5vw, 64px); line-height: 0.9; letter-spacing: -0.02em; font-weight: 400`.
`.db-atlas-h1 em`: `font-family: "Newsreader", serif; font-style: italic; color: var(--accent); font-weight: 400`.
`.db-atlas-lede`: same as `.al-atlas-lede`.
`.db-atlas-prose`: `margin-top: 36px; font-family: var(--serif); font-size: 14px; line-height: 1.7; color: var(--ink-mid)`.
`.db-atlas-prose p + p`: `margin-top: 14px`.
`.db-atlas-sidenotes`: `margin-top: 32px; padding-top: 16px; border-top: 1px solid var(--ink); font-family: var(--mono); font-size: 10.5px; letter-spacing: 0.04em; color: var(--ink-sub); display: grid; gap: 8px`.
`.db-atlas-sidenote-mark`: `color: var(--accent)`.
`.db-atlas-pipeline`: `margin-top: 28px; display: flex; gap: 6px; flex-wrap: wrap; font-family: var(--mono); font-size: 10px; letter-spacing: 0.14em; text-transform: uppercase; color: var(--ink-sub)`.
`.db-atlas-pipeline-arrow`: `margin: 0 6px; color: var(--accent)`.

**L. POST PAGE — Bottom folio** (scope: `html.theme-atlas .db-wrap`)

`.db-atlas-folio`: `margin: 0 36px; display: flex; justify-content: space-between; border-top: 1px solid var(--ink); padding: 10px 0 16px; font-family: var(--mono); font-size: 10px; letter-spacing: 0.18em; text-transform: uppercase; color: var(--ink-sub); margin-top: 48px`.

**M. HIDE EXISTING NIGHT-ONLY CHROME UNDER ATLAS** (scope: `html.theme-atlas`)

Under atlas, hide the existing dark-theme structural chrome that would look wrong on the light background. These are display overrides, not removals — the elements are still in the DOM for the night theme:
- `html.theme-atlas .db-bg`: `display: none` (the dark `radialGradient` scene background div)
- `html.theme-atlas .db-corner-panel`: reset border colors using `border-color: var(--ink)` and `background: var(--paper)` — these panels stay but adopt atlas palette. Do NOT hide them because they hold content used by the right-column logic.
- `html.theme-atlas .db-scene-bg, html.theme-atlas .al-scene-bg`: `display: none`
- The `.db-hero`, `.db-inline-toc`, `.db-sidebar`, `.db-main`, `.db-body` elements: set `background: transparent` so the atlas background shows through. Set `color: var(--ink)`.

**N. MOBILE RESPONSIVE** (scope: `html.theme-atlas`, breakpoint: `max-width: 768px`)

Inside `@media (max-width: 768px)`:
- `.al-atlas-body`: `grid-template-columns: 1fr; padding: 24px 20px 0`
- `.al-chart`: `height: 280px`
- `.al-atlas-h1`: `font-size: clamp(42px, 10vw, 80px)`
- `.atlas-index .ai-header-row, .atlas-index .ai-row`: `grid-template-columns: 28px 1fr 1.2fr 64px; gap: 8px` — hide Bortle and Type columns with `display: none` on the 5th and 6th children: `.atlas-index .ai-header-row > :nth-child(5), .atlas-index .ai-header-row > :nth-child(6), .atlas-index .ai-row > :nth-child(5), .atlas-index .ai-row > :nth-child(6): display: none`
- `.db-atlas-cols`: `grid-template-columns: 1fr; padding: 24px 20px`
- `.db-atlas-chart`: `height: 260px`
- `.al-ribbon, .db-atlas-ribbon`: `padding: 10px 16px; flex-wrap: wrap; gap: 8px`
- `.al-ribbon-left`: `gap: 12px; flex-wrap: wrap`
- `.al-atlas-folio, .db-atlas-folio`: `margin: 0 16px; flex-direction: column; gap: 4px`
  </action>
  <verify>
    <automated>grep -c "html\.theme-atlas" /Users/chaitanya/Documents/github/chaitanyakhoje.github.io/assets/css/astro-atlas.css</automated>
  </verify>
  <done>
- `assets/css/astro-atlas.css` exists
- Contains rule groups scoped under `html.theme-atlas .al-listing` (listing) and `html.theme-atlas .db-wrap` (post)
- Contains `.atlas-index .ai-row` grid template definition
- Contains `.db-atlas-cols` two-column grid definition
- Contains `@media (max-width: 768px)` block with stack overrides
- `grep -c "html\.theme-atlas" assets/css/astro-atlas.css` returns > 10
- No `:root` declarations (all tokens come from `astro-themes.css`)
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire astro-atlas.css into _includes/astro-head.html</name>
  <files>_includes/astro-head.html</files>
  <action>
Add a single `<link>` tag to `_includes/astro-head.html` immediately after the existing `astro-themes.css` link (line 4 currently). The new line must be:

`<link rel="stylesheet" href="{{ '/assets/css/astro-atlas.css' | relative_url }}" />`

Use 2-space indentation consistent with the rest of the file (the file uses no indentation currently — add no leading spaces, matching the existing link tag format). Do not touch any other content in the file.
  </action>
  <verify>
    <automated>grep "astro-atlas.css" /Users/chaitanya/Documents/github/chaitanyakhoje.github.io/_includes/astro-head.html</automated>
  </verify>
  <done>
- `_includes/astro-head.html` contains a line matching `astro-atlas.css` with `relative_url` filter
- The atlas CSS link appears after the `astro-themes.css` link (atlas rules load after tokens, correct cascade order)
- No other lines in the file were changed
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → CSS file | Static CSS served over HTTPS by GitHub Pages; no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | astro-atlas.css | accept | Static CSS file, no secrets or sensitive data; served over HTTPS |
| T-05-02 | Tampering | _includes/astro-head.html | accept | Build artifact; any change requires git commit access; GitHub Actions enforces CI |
</threat_model>

<verification>
After both tasks:

```bash
grep "astro-atlas.css" _includes/astro-head.html
grep -c "html\.theme-atlas" assets/css/astro-atlas.css
grep ":root" assets/css/astro-atlas.css   # must return nothing
```

The Jekyll build should pass:
```bash
bundle exec jekyll build
```
(Note: build requires the managed rbenv environment — verify locally per MEMORY.md.)
</verification>

<success_criteria>
- `assets/css/astro-atlas.css` exists with all listing and post atlas styles, minimum 200 lines
- All selectors scoped under `html.theme-atlas` — zero `:root` declarations
- `_includes/astro-head.html` contains the `astro-atlas.css` link after the `astro-themes.css` link
- `grep "astro-atlas.css" _includes/astro-head.html` returns 1 match
- `grep -c "html\.theme-atlas" assets/css/astro-atlas.css` returns > 10
</success_criteria>

<output>
After completion, create `.planning/phases/05-field-atlas/05-01-SUMMARY.md` using the summary template.
</output>
