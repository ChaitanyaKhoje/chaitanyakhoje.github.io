---
phase: 04-theme-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/css/astro-themes.css
autonomous: true
requirements:
  - THEME-01
  - THEME-03
  - JEKYLL-03
  - JEKYLL-04

must_haves:
  truths:
    - "Applying class .theme-atlas to <body> switches the page to the Field Atlas light palette"
    - "Applying class .theme-night to <body> switches the page to the After Dark dark palette"
    - "Token names are consistent across both theme blocks so Phase 5 and 6 can reference the same var() calls"
    - "No rule in astro-themes.css leaks onto elements outside .theme-atlas or .theme-night scope"
    - "portfolio.css and default.html are untouched — homepage styles are completely unaffected"
  artifacts:
    - path: assets/css/astro-themes.css
      provides: "CSS custom property blocks for both astro themes"
      contains: ".theme-atlas, .theme-night"
  key_links:
    - from: ".theme-atlas"
      to: "var(--paper), var(--ink), var(--accent)"
      via: "CSS custom properties"
      pattern: "\\.theme-atlas\\s*\\{"
    - from: ".theme-night"
      to: "var(--bg), var(--ink), var(--accent)"
      via: "CSS custom properties"
      pattern: "\\.theme-night\\s*\\{"
---

<objective>
Create `assets/css/astro-themes.css` containing the complete CSS custom property token blocks for both astrophotography themes: `.theme-atlas` (Field Atlas — light, cartographic cream) and `.theme-night` (After Dark — dark, cinematic near-black). This file is the single source of truth for all theme tokens consumed by Phase 5 and Phase 6 layouts.

Purpose: Establishes the design token foundation so Phase 5 (Field Atlas layout) and Phase 6 (After Dark layout) can reference the same `var(--token)` names without renegotiating values. Keeps `portfolio.css` completely untouched — all astro-specific tokens are isolated in this new file.

Output: `assets/css/astro-themes.css` with `.theme-atlas` and `.theme-night` blocks.
</objective>

<execution_context>
@/Users/chaitanya/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chaitanya/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-theme-foundation/04-CONTEXT.md

<interfaces>
<!-- Existing token pattern from portfolio.css (do NOT modify portfolio.css) -->
<!-- portfolio.css uses: :root / [data-theme="light"] / [data-theme="dark"] -->
<!-- astro-themes.css must use class selectors instead: .theme-atlas / .theme-night -->
<!-- This separation means the two token systems are fully independent -->

Token values to implement — sourced from /tmp/astro-blog-post/project/refresh.css:

.theme-atlas (Field Atlas — light):
  --paper: #ede6d3
  --paper-d: #ddd4be
  --ink: #1a1f2b
  --ink-mid: #4a4a4a
  --ink-sub: #786c54
  --rule: rgba(26,31,43,0.18)
  --rule-soft: rgba(26,31,43,0.08)
  --accent: #a04a2e        (burnt sienna)
  --indigo: #2c3a5a
  --mono: "JetBrains Mono", ui-monospace, monospace
  --serif: "Spectral", Georgia, serif
  --display: "Italiana", "Bodoni Moda", serif

.theme-night (After Dark — dark):
  --bg: #07080b
  --bg-2: #0d0f14
  --ink: #ece5d4
  --ink-mid: #a89f8a
  --ink-sub: #6a6357
  --rule: rgba(236,229,212,0.10)
  --accent: #d4a76a        (warm gold)
  --cyan: #6fc7d4
  --display: "Newsreader", "Times New Roman", serif
  --sans: "Space Grotesk", -apple-system, sans-serif
  --body: "Manrope", -apple-system, sans-serif
  --mono: "JetBrains Mono", ui-monospace, monospace
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create astro-themes.css with .theme-atlas and .theme-night token blocks</name>
  <files>assets/css/astro-themes.css</files>
  <read_first>
    - assets/css/portfolio.css (lines 1–30): Understand the existing token naming pattern; do NOT replicate or override it
    - .planning/phases/04-theme-foundation/04-CONTEXT.md: Decision details for scoping strategy
  </read_first>
  <action>
Create `assets/css/astro-themes.css` as a new file. The file must contain exactly two class-scoped custom property blocks.

Block 1 — `.theme-atlas` (Field Atlas, light theme):
  - Selector: `body.theme-atlas` (narrow scope to body so it does not pollute nested iframes or SVGs)
  - Properties (exact values):
    `--paper: #ede6d3`
    `--paper-d: #ddd4be`
    `--ink: #1a1f2b`
    `--ink-mid: #4a4a4a`
    `--ink-sub: #786c54`
    `--rule: rgba(26,31,43,0.18)`
    `--rule-soft: rgba(26,31,43,0.08)`
    `--accent: #a04a2e`
    `--indigo: #2c3a5a`
    `--mono: "JetBrains Mono", ui-monospace, monospace`
    `--serif: "Spectral", Georgia, serif`
    `--display: "Italiana", "Bodoni Moda", serif`

Block 2 — `.theme-night` (After Dark, dark theme):
  - Selector: `body.theme-night`
  - Properties (exact values):
    `--bg: #07080b`
    `--bg-2: #0d0f14`
    `--ink: #ece5d4`
    `--ink-mid: #a89f8a`
    `--ink-sub: #6a6357`
    `--rule: rgba(236,229,212,0.10)`
    `--accent: #d4a76a`
    `--cyan: #6fc7d4`
    `--display: "Newsreader", "Times New Roman", serif`
    `--sans: "Space Grotesk", -apple-system, sans-serif`
    `--body: "Manrope", -apple-system, sans-serif`
    `--mono: "JetBrains Mono", ui-monospace, monospace`

After the two theme blocks, add a third block for the default fallback (per D-06 in CONTEXT.md — dark is astrophotography default):

Block 3 — default body class when no theme signal exists:
  - Selector: `body.astro-page` (matches the existing body_class on astrophotography pages)
  - Content: a comment only: `/* default: .theme-night applied by JS on page load */`
  This block is intentionally empty of token declarations — the JS in Plan 02 applies the class before paint.

Do NOT add any layout, typography sizing, color application, or component styles. This file is token declarations only — Phase 5 and Phase 6 will add component-level CSS that references these vars.

Do NOT use `:root` — all tokens must be scoped to the class selectors so they cannot leak.

Add a comment header at the top of the file: `/* astro-themes.css — Design tokens for astrophotography pages (astro-theme system, independent of portfolio.css) */`
  </action>
  <verify>
    <automated>grep -c "body\.theme-atlas" /Users/chaitanya/Documents/github/chaitanyakhoje.github.io/assets/css/astro-themes.css && grep -c "body\.theme-night" /Users/chaitanya/Documents/github/chaitanyakhoje.github.io/assets/css/astro-themes.css && grep "paper: #ede6d3" /Users/chaitanya/Documents/github/chaitanyakhoje.github.io/assets/css/astro-themes.css && grep "bg: #07080b" /Users/chaitanya/Documents/github/chaitanyakhoje.github.io/assets/css/astro-themes.css</automated>
  </verify>
  <acceptance_criteria>
    - `assets/css/astro-themes.css` exists
    - Contains `body.theme-atlas { ... }` block with all 12 tokens listed above
    - Contains `body.theme-night { ... }` block with all 12 tokens listed above
    - No `:root` selector appears in the file
    - No layout, sizing, or component rules appear (token declarations only)
    - `grep "body.theme-atlas" assets/css/astro-themes.css` returns a match
    - `grep "body.theme-night" assets/css/astro-themes.css` returns a match
    - `grep ":root" assets/css/astro-themes.css` returns no match
    - `bundle exec jekyll build` succeeds (no build errors from the new CSS file)
  </acceptance_criteria>
  <done>astro-themes.css exists with correct token values in two class-scoped blocks; Jekyll build passes; portfolio.css is unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS file → browser | Static file served by GitHub Pages; no user input crosses this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | astro-themes.css | accept | Static file, no dynamic content; served over HTTPS by GitHub Pages |
| T-04-02 | Information Disclosure | CSS custom properties | accept | Token values are design constants, not secrets; visible in DevTools is expected |
</threat_model>

<verification>
1. `grep "body.theme-atlas" assets/css/astro-themes.css` — returns match
2. `grep "body.theme-night" assets/css/astro-themes.css` — returns match
3. `grep ":root" assets/css/astro-themes.css` — returns no match (tokens not on :root)
4. `grep -v "^\/\*" assets/css/portfolio.css | diff - assets/css/portfolio.css` — confirms portfolio.css is unchanged
5. `bundle exec jekyll build` — exits 0, no CSS-related errors
</verification>

<success_criteria>
- `assets/css/astro-themes.css` created with correct `.theme-atlas` and `.theme-night` class-scoped blocks
- All 12 Field Atlas tokens and all 12 After Dark tokens are present with exact values from the design source
- No token leaks onto `:root` — homepage styles cannot be affected
- Jekyll build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/04-theme-foundation/04-01-SUMMARY.md` following the summary template.
</output>
