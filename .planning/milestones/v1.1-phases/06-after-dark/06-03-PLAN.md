---
phase: 06-after-dark
plan: "03"
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - _layouts/astro-workflow.html
autonomous: true
requirements:
  - NIGHT-03
  - NIGHT-04
  - NIGHT-05
  - JEKYLL-01

<objective>
Add the After Dark post presentation to `_layouts/astro-workflow.html`. The new night-shell should render a wide cinematic hero, a minimal mast, a two-column body with prose and sidenotes, and a sparse footer while keeping the existing Field Atlas markup available for `html.theme-atlas`.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Add the After Dark post shell</name>
  <files>_layouts/astro-workflow.html</files>
  <action>
Insert a night-shell post layout that mirrors the `concept-night.jsx` prototype: minimal mast with back/index/session/next navigation, a full-bleed cinematic hero using `page.hero_image` with an SVG fallback, and a two-column body that renders `{{ content }}` plus gear / pipeline sidenotes. Keep the existing atlas markup in place but hidden under `html.theme-night`.
  </action>
  <verify>
    <automated>grep -c "night-mast\|night-hero\|night-body\|night-note" _layouts/astro-workflow.html</automated>
  </verify>
  <done>
- `_layouts/astro-workflow.html` contains the After Dark mast and hero shell
- The post body renders in a two-column structure
- The existing Field Atlas post layout remains intact for `html.theme-atlas`
  </done>
</task>
</tasks>

<verification>
After task completion:

```bash
grep -c "night-mast\|night-hero\|night-body\|night-note" _layouts/astro-workflow.html
grep "page.hero_image" _layouts/astro-workflow.html
```

Both commands must return results, and the file should remain valid HTML/Liquid.
</verification>

<success_criteria>
- `_layouts/astro-workflow.html` renders a cinematic After Dark post layout under `html.theme-night`
- `page.hero_image` is used when available, with a fallback SVG when not
- The body has the prose and sidenotes split the design calls for
- The page still builds cleanly under Jekyll
</success_criteria>
