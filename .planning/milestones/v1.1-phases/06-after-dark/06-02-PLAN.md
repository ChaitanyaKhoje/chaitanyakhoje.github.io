---
phase: 06-after-dark
plan: "02"
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - astrophotography.html
autonomous: true
requirements:
  - NIGHT-01
  - NIGHT-02
  - JEKYLL-02
  - JEKYLL-03

<objective>
Replace the visible dark-theme listing presentation in `astrophotography.html` with the After Dark feed-row shell from the night prototype. The atlas sections remain in the file for Field Atlas, but `html.theme-night` should show the new night mast, hero intro, feed rows, and footer while hiding the atlas and legacy night markup.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Add the After Dark listing shell</name>
  <files>astrophotography.html</files>
  <action>
Insert a night-shell listing structure that mirrors the `concept-night.jsx` prototype: a mast with `Khoje · after dark`, a large italic headline, a mono stats row, and a feed-row list generated from `astro_posts`. Use the existing post front matter to populate each row, including the thumbnail, designation, title, integration, subs, Bortle, and date.
  </action>
  <verify>
    <automated>grep -c "night-mast\|feed-row\|night-h1\|night-lede" astrophotography.html</automated>
  </verify>
  <done>
- `astrophotography.html` contains the After Dark mast and feed rows
- Each feed row links to the correct post URL
- The listing shell is additive and theme-scoped
  </done>
</task>
</tasks>

<verification>
After task completion:

```bash
grep -c "night-mast\|feed-row\|night-h1\|night-lede" astrophotography.html
grep "post.url | relative_url" astrophotography.html
```

Both commands must return results, and the file should remain valid HTML/Liquid.
</verification>

<success_criteria>
- `astrophotography.html` shows a full After Dark listing when `html.theme-night` is active
- The feed rows include thumbnails and session metadata
- The existing Field Atlas listing remains intact for `html.theme-atlas`
- The page still builds cleanly under Jekyll
</success_criteria>
