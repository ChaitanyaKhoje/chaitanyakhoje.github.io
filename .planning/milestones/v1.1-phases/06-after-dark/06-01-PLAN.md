---
phase: 06-after-dark
plan: "01"
type: execute
wave: 1
depends_on:
  - "05-03"
files_modified:
  - assets/css/astro-night.css
  - _includes/astro-head.html
autonomous: true
requirements:
  - NIGHT-01
  - NIGHT-02
  - NIGHT-03
  - NIGHT-04
  - NIGHT-05
  - JEKYLL-01

<objective>
Create the After Dark CSS foundation in `assets/css/astro-night.css` and wire it into `_includes/astro-head.html`. The stylesheet should scope all layout rules to `html.theme-night`, hide atlas / legacy night markup where necessary, and provide the typography, feed-row, hero, sidenote, and footer styles needed by the phase-6 HTML changes.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Add After Dark stylesheet and load it in astro-head</name>
  <files>assets/css/astro-night.css, _includes/astro-head.html</files>
  <action>
Create `assets/css/astro-night.css` with `html.theme-night`-scoped rules that implement the night shell, feed rows, hero, navigation, typography, and responsive behavior. Then add a stylesheet link in `_includes/astro-head.html` after `astro-atlas.css` so the night rules cascade after the shared tokens and atlas styles.
  </action>
  <verify>
    <automated>grep -c "theme-night" assets/css/astro-night.css</automated>
  </verify>
  <done>
- `assets/css/astro-night.css` exists and is scoped to `html.theme-night`
- `_includes/astro-head.html` loads the new stylesheet
- Night-specific classes are styled for both listing and post layouts
  </done>
</task>
</tasks>

<verification>
After task completion:

```bash
grep -c "theme-night" assets/css/astro-night.css
grep "astro-night.css" _includes/astro-head.html
```

Both commands must return results, and the file should remain valid CSS.
</verification>

<success_criteria>
- `assets/css/astro-night.css` exists and is loaded on astro pages
- Night layout rules are scoped to `html.theme-night`
- Atlas and legacy night markup can be hidden cleanly when the dark theme is active
- The stylesheet provides the typography and layout contract needed by the phase-6 HTML changes
</success_criteria>
