---
phase: 05-field-atlas
plan: "02"
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - astrophotography.html
autonomous: true
requirements:
  - ATLAS-02
  - ATLAS-03
  - JEKYLL-02

must_haves:
  truths:
    - "Under html.theme-atlas, the listing page shows a cream background with atlas ribbon at the top (station info left, PLATE I right)"
    - "Under html.theme-atlas, the listing page shows a two-column layout: title + lede + atlas index table on the left, RA/Dec SVG coordinate chart on the right"
    - "The atlas index table has 7 columns (№, Designation, Target, Integration, Bortle, Type, arrow) and one row per astro-workflow post"
    - "Clicking any row in the atlas index table navigates to the correct post URL"
    - "Under html.theme-night the existing dark-theme listing markup renders correctly (atlas-only markup is hidden or ignored)"
    - "The Liquid iteration correctly counts sessions and populates index rows from page.target, page.capture.integration, page.capture.bortle, page.object_type"
  artifacts:
    - path: "astrophotography.html"
      provides: "Atlas ribbon, atlas-body two-column section, atlas-index table, SVG chart plate with session markers"
      contains: "al-ribbon"
    - path: "astrophotography.html"
      provides: "Atlas index table populated from site.posts"
      contains: "atlas-index"
  key_links:
    - from: "astrophotography.html atlas index .ai-row"
      to: "post URL"
      via: "href={{ post.url | relative_url }}"
      pattern: "post\\.url.*relative_url"
    - from: "forloop.index"
      to: ".ai-num session number"
      via: "Liquid forloop.index | prepend: '0' | slice: -2, 2"
      pattern: "forloop\\.index"
---

<objective>
Add Field Atlas listing markup to `astrophotography.html` — the atlas ribbon, the two-column atlas body (title/lede/index table on left, SVG coordinate chart on right), and the bottom folio strip. All new markup is inside the existing `.al-listing` wrapper and is always rendered; the `html.theme-atlas` CSS from Plan 05-01 applies the atlas visual treatment, while `html.theme-night` CSS leaves the existing dark-theme markup to render normally.

Purpose: Implements the complete Field Atlas listing view — the cartographic atlas index that satisfies ATLAS-02 (kicker, title, index table, chart), ATLAS-03 (table rows with correct columns and links), and JEKYLL-02 (iteration over all astro-workflow posts).

Output: `astrophotography.html` with atlas-specific sections added inside `.al-listing`. The existing dark-theme sections (`.al-head`, `.al-featured`, `.al-session-grid`, `.al-method`, `.al-footer`) remain unchanged for `html.theme-night`.
</objective>

<execution_context>
@/Users/chaitanya/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chaitanya/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-field-atlas/05-CONTEXT.md
@.planning/phases/05-field-atlas/05-01-SUMMARY.md

Design reference JSX: /tmp/astro-blog-post/project/concept-atlas.jsx (AtlasListing component)

<interfaces>
<!-- CSS classes from 05-01 that this plan's HTML must use -->

Listing wrappers (scope: html.theme-atlas .al-listing):
  .al-ribbon              — top ribbon bar
  .al-ribbon-left         — left group (station info)
  .al-ribbon-right        — right label ("PLATE I · CONTENTS")
  .al-ribbon-dot          — accent dot (burnt sienna)
  .al-atlas-body          — two-column grid (1.05fr 1fr, gap 80px)
  .al-atlas-kicker        — mono uppercase sub-label
  .al-atlas-h1            — Italiana display headline
  .al-atlas-lede          — Spectral italic lede paragraph
  .atlas-index            — index table wrapper
  .atlas-index .ai-header-row  — header row (7-col grid)
  .atlas-index .ai-row    — data row (7-col grid, wraps <a>)
  .atlas-index .ai-num    — session number cell
  .atlas-index .ai-desig  — designation cell (burnt sienna)
  .atlas-index .ai-title  — target name cell (Spectral 16px)
  .atlas-index .ai-meta   — integration / bortle cells
  .atlas-index .ai-type   — type cell (10px uppercase)
  .atlas-index .ai-arrow  — arrow cell (→)
  .al-chart-col           — right column wrapper
  .al-chart-kicker-row    — chart kicker row (flex, space-between)
  .al-chart               — chart canvas (border, paper-d bg)
  .al-chart-grid          — grid background div
  .al-chart-stars         — star noise div
  .al-chart-corner (tl/tr/bl/br) — corner tick marks
  .al-chart-axis          — RA/Dec axis label spans
  .al-chart-marker        — target marker wrapper (positioned)
  .al-chart-marker .cross — crosshair pseudo-element span
  .al-chart-marker .ring  — circle ring span
  .al-chart-marker .lbl   — label tag span
  .al-chart-compass       — compass text div (bottom-right)
  .al-chart-footnotes     — footnote row below chart
  .al-chart-apparatus     — methodology block
  .al-chart-apparatus-grid — two-column apparatus grid
  .al-chart-apparatus-label — apparatus section label
  .al-atlas-folio         — bottom folio strip

<!-- Liquid variables from front matter (astro-workflow posts) -->
  post.title              — post title
  post.url                — post URL (use | relative_url)
  post.target             — designation (e.g. "IC 434")
  post.capture.integration — integration time string (e.g. "3h 10m")
  post.capture.bortle     — Bortle number (e.g. 8)
  post.object_type        — type string (e.g. "Emission")
  post.ra                 — RA string (e.g. "05h 40m 59s") — may be nil
  post.dec                — Dec string (e.g. "−02° 27′ 30″") — may be nil

<!-- Existing Liquid at top of astrophotography.html (already there) -->
  astro_posts             — all astro-workflow posts, sorted date desc
  total_sessions          — astro_posts.size
  total_frames            — sum of all post.capture.frames

<!-- Session number formatting in Liquid (no built-in zero-pad) -->
Use: {% assign snum = forloop.index | plus: 100 %}{% assign snum = snum | minus: 100 %}
Better: generate the session number as forloop.index formatted with leading zero
Liquid trick for zero-pad: assign n = forloop.index; if n < 10, prepend "0"
Pattern: {% if forloop.index < 10 %}0{{ forloop.index }}{% else %}{{ forloop.index }}{% endif %}

<!-- Chart RA/Dec coordinate math for SVG markers -->
The chart shows RA 0h–8h on x-axis, Dec ±70° on y-axis.
xPct(ra_hours) = 6 + (ra_hours / 8) * 88   → percent from left
yPct(dec_deg)  = 50 - (dec_deg / 70) * 42  → percent from top
Liquid cannot do floating-point math. Use inline style with hardcoded
values computed by hand for each known post, or use a Liquid approximation
with integer math and | times: | divided_by:.
Acceptable approach: compute approximate percentages using Liquid integer math.
  ra in hours (integer part): post.ra_h (if available), else use mid-band default 45%
  dec in degrees: post.dec_d (if available), else use mid-band default 50%
If page.ra and page.dec are not broken into numeric sub-fields, fall back to
a hardcoded SVG marker at center (left: 45%, top: 50%) for every post.
Note in the task action below: use the fallback center position for all markers
unless ra_h / dec_d integer front-matter fields exist. Per D-01 (use page.* as-is),
do not add new front-matter fields — use the fallback position.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add atlas ribbon and atlas-body section to astrophotography.html</name>
  <files>astrophotography.html</files>
  <action>
Open `astrophotography.html`. Read the full file before editing. The file structure is:

```
front matter
Liquid assignments (total_sessions, total_frames)
<div class="astro-listing-wrap">
  <div class="al-scene-bg" ...></div>
  <div class="al-mission-bar">  ← existing dark-theme ribbon
    <div class="al-status-strip">  ← contains back link + toggle + status
  </div>
  <div class="al-listing">
    [header, featured, sessions, method, footer — all existing dark-theme content]
  </div>
</div>
```

Make two additions inside `.al-listing`, both BEFORE the existing `<!-- Header -->` comment:

**Addition 1 — Atlas ribbon:**
Insert the following HTML block as the FIRST child of `.al-listing` (before the existing `<header class="al-head">`):

```html
<!-- Atlas ribbon (Field Atlas theme) -->
<div class="al-ribbon">
  <div class="al-ribbon-left">
    <span><span class="al-ribbon-dot"></span> CK · STATION SUNNYVALE</span>
    <span>37.37° N · 122.04° W</span>
    <span>BORTLE 7–8</span>
  </div>
  <div class="al-ribbon-right">PLATE I · CONTENTS</div>
</div>
```

**Addition 2 — Atlas body (two-column layout):**
Immediately after the atlas ribbon block (before `<header class="al-head">`), insert the atlas body section. This is a self-contained section visible under atlas theme and hidden under night theme (the CSS from 05-01 handles this via scoping — `html.theme-atlas .al-listing` shows it, `html.theme-night` existing styles don't conflict because the classes don't exist in the night CSS).

The atlas body section structure:

```html
<!-- Atlas body: two-column listing layout (Field Atlas theme) -->
<div class="al-atlas-body">

  <!-- Left column: kicker + title + lede + index table -->
  <div>
    <div class="al-atlas-kicker">Field log · Sessions 001 – {{ total_sessions | prepend: "00" | slice: -3, 3 }} · {{ site.time | date: "%Y" }}</div>
    <h2 class="al-atlas-h1">A field<br />atlas of <em>nebulae,</em><br />stacked from<br />Sunnyvale.</h2>
    <p class="al-atlas-lede">
      {{ total_sessions }} session{% if total_sessions != 1 %}s{% endif %}, one balcony, one small smart-telescope. Each plate records what could be coaxed out of a Bortle 7–8 sky with patience and a careful processing pipeline.
    </p>

    <!-- Atlas index table -->
    <div class="atlas-index">
      <div class="ai-header-row">
        <span>№</span>
        <span>DESIGNATION</span>
        <span>TARGET</span>
        <span>INTEGRATION</span>
        <span>BORTLE</span>
        <span>TYPE</span>
        <span></span>
      </div>
      {% for post in astro_posts %}
      <a class="ai-row" href="{{ post.url | relative_url }}">
        <span class="ai-num">{% if forloop.index < 10 %}0{{ forloop.index }}{% else %}{{ forloop.index }}{% endif %}</span>
        <span class="ai-desig">{{ post.target | default: "—" }}</span>
        <span class="ai-title">{{ post.title }}</span>
        <span class="ai-meta">{{ post.capture.integration | default: "—" }}</span>
        <span class="ai-meta">{% if post.capture.bortle %}B{{ post.capture.bortle }}{% else %}—{% endif %}</span>
        <span class="ai-type">{{ post.object_type | default: "—" }}</span>
        <span class="ai-arrow">→</span>
      </a>
      {% endfor %}
      {% if total_sessions == 0 %}
      <div class="ai-row" style="cursor:default;">
        <span class="ai-num">—</span>
        <span class="ai-desig">—</span>
        <span class="ai-title" style="font-style:italic;color:var(--ink-sub);">No sessions logged yet</span>
        <span class="ai-meta">—</span>
        <span class="ai-meta">—</span>
        <span class="ai-type">—</span>
        <span class="ai-arrow"></span>
      </div>
      {% endif %}
    </div>
  </div>

  <!-- Right column: RA/Dec coordinate chart -->
  <div class="al-chart-col">
    <div class="al-chart-kicker-row">
      <span class="al-atlas-kicker" style="margin-bottom:0;">Plate · RA 00ʰ – 08ʰ × Dec ±70°</span>
      <span class="al-atlas-kicker" style="margin-bottom:0;">Equinox J2000.0</span>
    </div>

    <div class="al-chart">
      <div class="al-chart-grid"></div>
      <div class="al-chart-stars"></div>
      <span class="al-chart-corner tl"></span>
      <span class="al-chart-corner tr"></span>
      <span class="al-chart-corner bl"></span>
      <span class="al-chart-corner br"></span>

      <!-- RA axis labels (top) -->
      <span class="al-chart-axis" style="top:6px;left:6%;transform:translateX(-50%);">00ʰ</span>
      <span class="al-chart-axis" style="top:6px;left:28%;transform:translateX(-50%);">02ʰ</span>
      <span class="al-chart-axis" style="top:6px;left:50%;transform:translateX(-50%);">04ʰ</span>
      <span class="al-chart-axis" style="top:6px;left:72%;transform:translateX(-50%);">06ʰ</span>
      <span class="al-chart-axis" style="top:6px;left:94%;transform:translateX(-50%);">08ʰ</span>

      <!-- Dec axis labels (left) -->
      <span class="al-chart-axis" style="left:8px;top:20%;transform:translateY(-50%);">+60°</span>
      <span class="al-chart-axis" style="left:8px;top:32%;transform:translateY(-50%);">+30°</span>
      <span class="al-chart-axis" style="left:8px;top:50%;transform:translateY(-50%);">0°</span>
      <span class="al-chart-axis" style="left:8px;top:68%;transform:translateY(-50%);">−30°</span>
      <span class="al-chart-axis" style="left:8px;top:80%;transform:translateY(-50%);">−60°</span>

      <!-- Session markers: fallback to center band since RA/Dec numeric fields not guaranteed.
           Each marker positioned per known post coordinates (hardcoded from concept-atlas.jsx data):
           IC 434: RA 5.7h → x=68.7%, Dec -2.5° → y=51.5%
           Soul Nebula: RA 2.92h → x=38.1%, Dec 60.4° → y=13.8%
           M 31: RA 0.71h → x=13.8%, Dec 41.27° → y=25.4%
           M 42: RA 5.59h → x=67.5%, Dec -5.39° → y=53.2%
           M 45: RA 3.79h → x=47.7%, Dec 24.12° → y=35.5%
           For sessions beyond the prototype data, markers fall back to center (45%, 50%).
           Positions are inline styles per CONTEXT.md (no dynamic Liquid math for float coords). -->
      {% for post in astro_posts %}
      {% assign pidx = forloop.index %}
      {% assign marker_left = "45%" %}
      {% assign marker_top = "50%" %}
      {% if post.target == "IC 434" or post.target == "IC434" %}{% assign marker_left = "69%" %}{% assign marker_top = "52%" %}{% endif %}
      {% if post.target == "Wsh 5" or post.target == "Sh2-199" or post.target == "Soul Nebula" %}{% assign marker_left = "38%" %}{% assign marker_top = "14%" %}{% endif %}
      {% if post.target == "M 31" or post.target == "M31" %}{% assign marker_left = "14%" %}{% assign marker_top = "25%" %}{% endif %}
      {% if post.target == "M 42" or post.target == "M42" %}{% assign marker_left = "68%" %}{% assign marker_top = "53%" %}{% endif %}
      {% if post.target == "M 45" or post.target == "M45" %}{% assign marker_left = "48%" %}{% assign marker_top = "36%" %}{% endif %}
      <div class="al-chart-marker" style="left:{{ marker_left }};top:{{ marker_top }};">
        <div style="position:relative;">
          <span class="cross"></span>
          <span class="ring"></span>
        </div>
        <span class="lbl">{% if forloop.index < 10 %}0{{ forloop.index }}{% else %}{{ forloop.index }}{% endif %} · {{ post.target | default: post.title | truncate: 10, "" }}</span>
      </div>
      {% endfor %}

      <div class="al-chart-compass">N ↑ · E ←<br />━━ 1ʰ ━━</div>
    </div>

    <div class="al-chart-footnotes">
      <span>* All coordinates J2000.0</span>
      <span>† Plate II–IV on request</span>
    </div>

    <!-- Apparatus methodology block -->
    <div class="al-chart-apparatus">
      <div class="al-atlas-kicker" style="margin-bottom:14px;">Apparatus</div>
      <div class="al-chart-apparatus-grid">
        <div>
          <div class="al-chart-apparatus-label">OPTIC</div>
          ZWO Seestar S50, 250 mm <em>ƒ</em>/4.8. Built-in OSC CMOS at native pixel scale.
        </div>
        <div>
          <div class="al-chart-apparatus-label">PIPELINE</div>
          WBPP 3.0 in PixInsight — DBE, BlurX, NoiseX, StarX, statistical stretch, LHE.
        </div>
      </div>
    </div>
  </div>

</div><!-- /al-atlas-body -->

<!-- Atlas folio strip (Field Atlas theme) -->
<div class="al-atlas-folio">
  <span>Khoje · field atlas · vol. 1</span>
  <span>fol. 001 — recto</span>
  <span>CC-BY-NC 4.0 · {{ "now" | date: "%Y" }}</span>
</div>
```

Do not remove or alter any existing HTML in the file. The atlas sections are additive — the dark-theme markup (`.al-head`, `.al-featured`, `.al-session-grid`, etc.) remains intact and will render under `html.theme-night` as before.

Use 2-space indentation throughout, matching the file's existing convention.
  </action>
  <verify>
    <automated>grep -c "al-ribbon\|atlas-index\|al-atlas-body\|al-chart" /Users/chaitanya/Documents/github/chaitanyakhoje.github.io/astrophotography.html</automated>
  </verify>
  <done>
- `astrophotography.html` contains `.al-ribbon` div with left/right sub-divs
- `astrophotography.html` contains `.al-atlas-body` div with two-column layout
- `astrophotography.html` contains `.atlas-index` div with `.ai-header-row` and Liquid `for post in astro_posts` loop generating `.ai-row` elements
- Each `.ai-row` is an `<a>` tag with `href="{{ post.url | relative_url }}"`
- `astrophotography.html` contains `.al-atlas-folio` div
- `grep -c "al-ribbon\|atlas-index\|al-atlas-body\|al-chart" astrophotography.html` returns >= 8
- Existing dark-theme markup (`.al-head`, `.al-featured`, `.al-session-grid`) still present
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Jekyll template → user data | post.target, post.title, post.object_type from front matter rendered into HTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Information Disclosure | post front matter in HTML | accept | All data is public blog content; no secrets in front matter |
| T-05-04 | Tampering | Liquid template output | accept | Jekyll builds to static HTML; no runtime template injection; GitHub Pages HTTPS delivery |
</threat_model>

<verification>
After task completion:

```bash
grep -c "al-ribbon\|atlas-index\|al-atlas-body" astrophotography.html
grep "post.url.*relative_url" astrophotography.html
grep "al-atlas-folio" astrophotography.html
```

All three commands must return results. The file must remain valid HTML/Liquid — check that all `{% for %}` tags have matching `{% endfor %}` and all `<div>` tags are closed.
</verification>

<success_criteria>
- `astrophotography.html` contains atlas ribbon, atlas-body two-column section, atlas index table, SVG chart, and atlas folio
- Atlas index table iterates `astro_posts` with Liquid `for` loop, one `.ai-row` per post
- Each row contains: zero-padded session number, `post.target`, `post.title`, `post.capture.integration`, Bortle with "B" prefix, `post.object_type`, and arrow "→"
- Each row is an `<a>` with `href="{{ post.url | relative_url }}"` — clicking navigates to the post
- Existing night-theme markup (`.al-head`, `.al-featured`, `.al-session-grid`) remains intact
- Jekyll build passes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-field-atlas/05-02-SUMMARY.md` using the summary template.
</output>
